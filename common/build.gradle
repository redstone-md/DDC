architectury {
    common(rootProject.enabled_platforms.split(","))
}

loom {
    accessWidenerPath = file("src/main/resources/ddc.accesswidener")
}

dependencies {
    // Shared dependencies across Fabric and NeoForge
    modImplementation "dev.architectury:architectury:9.2.14" // example version for 26.1.x
}
